services:
  - type: web
    name: kannrennzu-app
    env: python
    buildCommand: |
      apt-get update
      apt-get install -y graphviz
      pip install -r requirements.txt
    startCommand: gunicorn app:app