services:
  - type: web
    name: kannrennzu-app
    env: python

    buildCommand: |
      pip install -r requirements.txt
      apt-get update
      apt-get install -y graphviz

    startCommand: gunicorn app:app